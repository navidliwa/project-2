const User = require('./user');
const Location = require('./location');
const Character = require('./character');
const Plot = require('./plot');

// data table relationships here

module.exports = {User, Location, Character, Plot}
<div class="container">
    <div class="card-deck">
        {{#each characters as |character|}}
        <div class="card">
            <div class="card-body">
                <div class="card-title">{{character.name}}</div>
                <div class="card-text">
                    {{character.description}}
                </div>
                <a class="btn btn-primary" href="/update/character/{{character.id}}">Edit</a>
                <button class="btn btn-danger" data-id="{{character.id}}">Delete</button>
            </div>
            <img src="{{character.image}}" alt="{{character.name}} image" class="card-img-bottom">
        </div>
        {{/each}}
    </div>
</div>
<div class="container">
    <div class="row">
        <div class="col-6">
            <div class="col-6">{{name}}</div>
            <div class="col">{{description}}
            </div>
        </div>
        <div class="col-6">
            <img src="{{image}}" alt="{{name}} image">
        </div>
    </div>
</div>